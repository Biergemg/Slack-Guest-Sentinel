# ============================================================
# Slack Guest Sentinel — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control.
#
# All variables marked with * are REQUIRED.
# Missing required variables will cause the app to fail at startup
# with a clear error message.
# ============================================================

# ------------------------------------------------------------
# Supabase
# ------------------------------------------------------------

# * Your Supabase project URL (visible in Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# * Supabase Service Role Key — keeps server-side only, never expose to client
# Found in: Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ------------------------------------------------------------
# Slack App
# ------------------------------------------------------------
# Create a Slack App at https://api.slack.com/apps
# and configure it using slack_manifest.json

# * Slack App Client ID (from Basic Information > App Credentials)
SLACK_CLIENT_ID=your-slack-client-id

# * Slack App Client Secret (from Basic Information > App Credentials)
SLACK_CLIENT_SECRET=your-slack-client-secret

# * Slack Signing Secret — used to verify request authenticity
# Found in: Basic Information > App Credentials > Signing Secret
SLACK_SIGNING_SECRET=your-slack-signing-secret

# ------------------------------------------------------------
# Stripe
# ------------------------------------------------------------
# Create products at https://dashboard.stripe.com/products

# * Stripe Secret Key — use sk_test_... for development, sk_live_... for production
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# * Stripe Webhook Secret — from Stripe Dashboard > Webhooks > your endpoint
# For local testing, use: stripe listen --forward-to localhost:3000/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# * Stripe Price IDs for recurring subscriptions
# Create in: Stripe Dashboard > Products > Add product > Add price
STRIPE_PRICE_STARTER=price_your-starter-price-id
STRIPE_PRICE_GROWTH=price_your-growth-price-id
STRIPE_PRICE_SCALE=price_your-scale-price-id

# ------------------------------------------------------------
# Application
# ------------------------------------------------------------

# * Public URL of the app (no trailing slash)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# * AES-256 Encryption Key for Slack tokens stored in the database
# MUST be exactly 32 bytes (characters).
# Generate a secure key:
# Generate a secure 32-byte key (64 hex characters):
#   openssl rand -hex 32
#
# IMPORTANT: Changing this key makes all existing encrypted data unreadable.
# Store this securely — loss means losing all customer tokens.
ENCRYPTION_KEY=4f8d6c2c8f0b8b77b8c9c3f7d6a2c1e0c4a6e7f8b9d0c1a2e3f4b5c6d7e8f9a0

# * Secret for authenticating internal cron job requests
# Generate: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here
